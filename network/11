#using the command
traceroute slash16.org
#on the first line you can see Local ip, and on the second line you se NAT(Network address translation)
#192.168.2.9
#It's router, which remaps the local IP to a Public IP
